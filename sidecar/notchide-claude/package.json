{
  "name": "notchide-claude",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HOST-mode AAP sidecar bridging the Claude Agent SDK to notchide's unix socket.",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.207"
  }
}
